<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Phase 1 spike module. It proves the module pattern (the module owns its endpoints and exposes
    one Map...Endpoints extension) and .NET 10 built-in validation from a module library (ADR 0003).
    It contains no business logic and is deleted when the first real module arrives.
  -->

  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

</Project>
